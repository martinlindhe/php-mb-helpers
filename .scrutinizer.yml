filter:
    excluded_paths:
        - 'tests/*'
checks:
    php:
        code_rating: true
        duplication: true

build:
    tests:
        override:
            -
                command: 'phpunit --coverage-clover=coverage.tmp'
                coverage:
                    file: 'coverage.tmp'
                    format: 'php-clover'
